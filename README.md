# 42_multilayer_perceptron
 an introduction to artificial neural networks, with the implementation of a multilayer perceptron.

The activation functions were implemented as a part of layer (each layer output has to go through activation transformation). Which is wrong, layer in the documentation I saw that we implement them as separate layer. The project doesn't do that